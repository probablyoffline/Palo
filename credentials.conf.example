# Copy this file to ~/.palo/credentials.conf and fill in your values.
# mkdir -p ~/.palo && cp credentials.conf.example ~/.palo/credentials.conf

[palo]
firewall = your-firewall-hostname-or-ip
api_key  = your-panos-api-key-here
